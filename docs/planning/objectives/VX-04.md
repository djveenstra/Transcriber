# VX-04 — Extract Orchestration Seams from TranscriptionSession

Status: Complete

Risk tier: High

Target app: Transcriber Mac

Manager: Codex

Approved to start by Daniel: Yes — sequential Phase 1 authorization on 2026-07-29

## Mission

Extract small, testable final-transcription, diarization-attempt, and persistence coordination seams from `TranscriptionSession` without changing user-visible behavior, data meaning, storage, models, dependencies, or processing states.

## Why now

- Prerequisite: Phase 0 milestone `PROCEED`; VX-03 principles approved.
- Primary risk: R-03.
- Current evidence: `TranscriptionSession.swift` is 1,927 lines and combines UI state with engine orchestration, watchdog, and persistence, while its existing cancellation/failure behavior is heavily tested.

## Scope

### Allowed paths

- `src/native/Transcriber2/Transcriber/TranscriptionSession.swift`
- New narrowly named orchestration service/protocol files under `src/native/Transcriber2/Transcriber/`
- `src/native/Transcriber2/TranscriberTests/TranscriptionSessionTests.swift`
- New focused tests under `src/native/Transcriber2/TranscriberTests/`
- `docs/planning/objectives/VX-04.md`
- `docs/planning/evidence/VX-04-REPORT.md`
- `OBJECTIVE.md`
- `PLAN.md`
- `DECISIONS.md` only if a new non-obvious decision is required
- `QA.md`

### Forbidden changes

- No `Recording` or SwiftData schema change.
- No artifact store, persistent job, audio derivative, new product state, model, dependency, package pin, project setting, entitlement, or UI redesign.
- No project-file change is expected because source groups are filesystem-synchronized.
- No VX-02 cleanup or edits to iOS, Python, legacy, asset, private-audio, application-data, model-cache, or research trees.

### Private-data permission

None. Tests use fakes, synthetic values, and temporary files only.

## Required behavior preservation

- Audio/import persistence before dependent work.
- Transcript persistence before diarization.
- Cancellation and retry behavior.
- Stale-attempt protection.
- Diarization timeout, unsafe-overlap guard, and fallback behavior.
- Sequential model unload/pacing before Mac diarization.
- Diagnostics, activity, completion/failure messages, playback/export compatibility, and existing UI states.

## Implementation tasks

- [x] Add characterization tests for extracted decision/persistence seams where existing coverage is indirect.
- [x] Extract a final-transcription runner behind a protocol without changing model selection or pacing.
- [x] Extract diarization attempt/watchdog coordination behind a protocol while preserving guards and timeouts.
- [x] Extract persistence-save coordination behind a small protocol/value without changing save order or `Recording` fields.
- [x] Keep `TranscriptionSession` as the `@MainActor` UI-facing state owner.
- [x] Run focused and full validation.
- [x] Obtain independent Auditor `ALIGNED`.
- [x] Obtain independent QA PASS.

## Data and migration

No schema, artifact format, application data, or migration change. Existing reads/writes remain byte- and behavior-compatible through the same `Recording` fields.

## Validation

Run the PLAN §4 Mac build and full `TranscriberTests`, plus focused `TranscriptionSession` and new seam tests. Verify `git diff --check`, dependency pins, strict concurrency, and allowed paths.

## Acceptance criteria

- [ ] The three responsibilities have explicit seams and focused tests.
- [ ] No wholesale rewrite or UI/state/storage behavior change.
- [ ] Existing cancellation, stale-attempt, timeout, fallback, persist-first, playback, and export tests pass.
- [ ] Mac build and full unit bundle pass.
- [ ] Strict concurrency and dependency pins are unchanged.
- [ ] Auditor returns `ALIGNED`; QA returns PASS.
- [ ] Rollback is the direct restoration of extracted code into the prior session implementation with no data conversion.

## Gate

`PROCEED` — 2026-07-29.

Worker completed the narrow extraction. Auditor returned `ALIGNED`. Independent QA passed 25/25 focused tests; Manager validation passed the Mac build and 167/167 full tests. No Human-only gate remains.

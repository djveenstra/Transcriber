# VX-02 — Mac-Only Boundary and Removal Inventory

Status: Complete

Risk tier: Docs-only with read-only cross-workspace ownership inspection

Target app: Transcriber Mac

Manager: Codex

Approved to start by Daniel: Yes — blanket Phase 0 authorization on 2026-07-29

## Mission

Remove ambiguity before any cleanup by proving Mac/iOS ownership and classifying possible future removal targets without moving, deleting, or changing application files.

## Why now

- Prerequisite objectives: VX-01 `PROCEED`.
- Risks and gaps addressed: R-23, R-24, R-28 and Q-08.
- Current evidence: the Mac and iOS workspaces were recently soft-split, but this Mac checkout still contains iOS targets, platform branches, mobile-only engine code, historical trees, duplicated governance, and unresolved root audio.

## Scope

### Allowed paths

- `docs/planning/objectives/VX-02.md`
- `docs/planning/evidence/VX-02-BOUNDARY-INVENTORY.md`
- `OBJECTIVE.md`
- `PLAN.md`
- `DECISIONS.md`
- `QA.md`

Read-only inspection is allowed in:

- this repository, including `src/native/Transcriber2/` and `src/legacy-ios/`;
- `../iOS Transcriber/` for ownership and byte-equivalence evidence;
- Git metadata for both workspaces.

### Forbidden paths and changes

- No production, test, project, entitlement, plist, asset, historical-tree, audio, dependency, model, or application-data changes.
- No changes of any kind in `../iOS Transcriber/`, `../Python Transcriber/`, or `Voiceprint-downloads/`.
- No file deletion, movement, archive operation, staging, commit, or push.
- No private audio content or extended metadata inspection.

### Private-data permission

None. Root audio may be listed by already-known filename and path presence only. It must not be opened, played, hashed, copied, transcribed, probed, or otherwise inspected.

### In scope

- Verify the sibling workspace’s iOS product ownership and compare relevant shared application files.
- Inventory Xcode iOS/share-extension targets, plists, entitlements, platform branches, mobile-only model/audio code, legacy trees, stale assets/build trees, duplicate governance, and root audio fixtures.
- Classify each candidate as retain, move to sibling, archive, remove later, or unresolved.
- Provide exact proof requirements, risks, rollback, and proposed future mechanical objectives.

### Out of scope

- Removing, moving, archiving, editing, or deduplicating any application or reference file.
- Deciding that shared byte-equivalence alone makes deletion safe.
- Modifying the sibling iOS workspace.
- Phase 1 implementation.

### Behavior that must remain unchanged

All Mac production behavior and all sibling iOS behavior. Existing user data and private audio remain untouched.

## Technical approach

Use path inventories, Xcode project references, platform-conditional searches, file comparisons, and Git status. A candidate receives a removal classification only when its owner, references, unique value, validation plan, and recovery path are explicit. Otherwise it remains unresolved.

## Data and migration

- Existing data read path: not accessed.
- New data/write path: documentation only.
- Versioning: inventory carries a date and source-worktree boundary.
- Corruption behavior: not applicable.
- Rollback compatibility: revert the documentation and pointers; no app or user data changes.
- User-data deletion: none.

## Implementation tasks

- [x] Capture both worktree boundaries without modifying either.
- [x] Verify sibling iOS ownership and relevant file equivalence/differences.
- [x] Inventory Xcode targets, plist/entitlements, platform branches, model/audio code, legacy/reference trees, assets, governance duplication, and root audio.
- [x] Classify every candidate with proof, risk, future action, and recovery requirements.
- [x] Propose mechanical follow-up objectives without executing removal.
- [x] Validate scope, links, diff, and private-data absence.

## Validation

### Baseline

VX-02 changes no code. It relies on the green VX-01 build/test baseline and reruns documentation/scope checks.

### Objective-specific automated checks

```sh
git status --short
git diff --check
rg -n "#if os\\(iOS\\)|#elseif os\\(iOS\\)|targetEnvironment\\(macCatalyst\\)" \
  src/native/Transcriber2
rg -n "ShareToTranscriber|SUPPORTED_PLATFORMS|TARGETED_DEVICE_FAMILY" \
  src/native/Transcriber2/Transcriber2.xcodeproj/project.pbxproj
diff -qr src/native/Transcriber2 "../iOS Transcriber/src/native/Transcriber2"
```

### Benchmark

Not applicable.

### Manual Mac checks

None. No product changes.

### Human-owned checks

Every unresolved deletion, movement, or archive choice remains for Daniel. Inventory completion does not authorize cleanup.

## Acceptance criteria

- [x] Sibling iOS ownership and comparison evidence are recorded.
- [x] Candidate application/reference/governance/asset/audio items are classified.
- [x] No application, sibling, private-audio, user-data, dependency, or model content changes.
- [x] Every unresolved ownership or deletion choice is explicit.
- [x] QA evidence is appended.

## Removal targets

None in VX-02. This objective only inventories and proposes future scoped removals.

## Rollback

Revert the VX-02 documentation and governance pointer updates. Neither application workspace nor user data is changed.

## Notes for the Manager

- The sibling native tree is byte-identical but its workspace root is not a Git repository; durable ownership should precede structural removal.
- The share extension is iOS-only, but the app-group entitlement and shared-inbox UI are cross-platform and require a separate product decision.
- Root audio remains unapproved and uninspected.

---

## Manager report

The complete classification, proof requirements, recovery rules, proposed mechanical objectives, and nine Human decisions are recorded in [VX-02-BOUNDARY-INVENTORY.md](../evidence/VX-02-BOUNDARY-INVENTORY.md). No application file, project, sibling file, historical tree, asset, build artifact, audio file, model, dependency, or user data changed.

## Auditor report

Not required by tier unless risk or scope changes.

## QA evidence

PARTIAL: the read-only inventory passed, while PLAN requires Human decisions for unresolved ownership/removal choices. See [QA.md](../../../QA.md#vx-02--mac-only-boundary-and-removal-inventory--2026-07-29).

## Gate decision

`PROCEED` — 2026-07-29.

Daniel approved the conservative disposition: retain every candidate in place and defer all cleanup, movement, archival, audio use, and Mac/iOS project narrowing. No proposed cleanup is authorized or performed.
